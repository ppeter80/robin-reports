# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Local Jekyll bundle (we use GitHub Pages cloud build, not local)
vendor/
.bundle/
Gemfile.lock

# Editor / OS junk
.DS_Store
Thumbs.db
*.swp
